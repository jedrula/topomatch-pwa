# Firebase Emulator Data

This directory contains exported data from Firebase emulators to persist data between sessions.

## Structure

- `firestore_export/` - Firestore database exports
- `storage_export/` - Storage bucket exports
- `auth_export/` - Authentication exports

The emulators will automatically import from and export to this directory.
