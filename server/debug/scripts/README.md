# Debug Scripts

Firebase inspection utilities for debugging diagnostic reports and server responses.

## Usage

All scripts use Firebase Admin SDK and require service account credentials:

```bash
GOOGLE_APPLICATION_CREDENTIALS=./topomatch-pwa-firebase-adminsdk.json node debug/scripts/<script-name>.js <arguments>
```

See individual script files for specific usage and options.

